<h1>Categories</h1>

<%= link_to 'New Category', new_category_path %>

<ul>
  <% @categories.each do |category| %>
    <li>
        <%= category.name %>
    </li>
  <% end %>
</ul>

<%= link_to 'Back', seller_path(@seller) %>